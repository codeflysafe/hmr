import torch
import torch.nn as nn

from encoder import build_encoder

class HMR(nn.Module):
    def __init__(self, cfg) -> None:
        super().__init__()


    def forward(self, X):
        pass
    
